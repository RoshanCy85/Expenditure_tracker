<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Transactions - <%= category %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-6">
    <div class="max-w-3xl mx-auto bg-white shadow-md rounded-lg p-6">
        <h1 class="text-2xl font-bold text-amber-800 mb-4">Transactions - <%= category %></h1>

        <table class="w-full border-collapse border border-gray-300 bg-white rounded-lg shadow-md">
            <thead>
                <tr class="bg-gray-200 text-left">
                    <th class="border border-gray-300 px-4 py-2">Date</th>
                    <th class="border border-gray-300 px-4 py-2">Amount (₹)</th>
                </tr>
            </thead>
            <tbody>
                <% if (transactions.length > 0) { %>
                    <% transactions.forEach(transaction => { %>
                        <tr class="hover:bg-gray-100 transition">
                            <td class="border border-gray-300 px-4 py-2"><%= transaction.created_at.toISOString().split('T')[0] %></td>
                            <td class="border border-gray-300 px-4 py-2">₹<%= transaction.amount %></td>
                        </tr>
                    <% }); %>
                <% } else { %>
                    <tr>
                        <td colspan="2" class="text-center py-4 text-gray-500">No transactions found.</td>
                    </tr>
                <% } %>
                
                <!-- Total Row -->
                <tr class="bg-gray-100 font-bold">
                    <td class="border border-gray-300 px-4 py-2 text-right">Total:</td>
                    <td class="border border-gray-300 px-4 py-2">₹<%= totalAmount %></td>
                </tr>
            </tbody>
        </table>

        <!-- Back Button -->
        <div class="mt-6">
            <a href="/" class="inline-block px-4 py-2 bg-amber-600 text-white rounded-lg hover:bg-amber-700 transition">Back to Home</a>
        </div>
    </div>
</body>
</html>
